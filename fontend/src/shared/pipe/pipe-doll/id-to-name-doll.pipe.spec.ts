import { IdToNameDollPipe } from './id-to-name-doll.pipe';

describe('IdToNameDollPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToNameDollPipe();
    expect(pipe).toBeTruthy();
  });
});
